<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <!-- Application tests that require real PostgreSQL semantics (ADR-006).
         They run against a dedicated test database, never against the
         development database. Values below are local development placeholders. -->
    <testsuites>
        <testsuite name="PostgreSQL">
            <directory>tests/PostgreSQL</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="pgsql"/>
        <env name="DB_HOST" value="postgres"/>
        <env name="DB_PORT" value="5432"/>
        <env name="DB_DATABASE" value="stewardarc_test"/>
        <env name="DB_USERNAME" value="stewardarc"/>
        <env name="DB_PASSWORD" value="stewardarc"/>
        <!-- Failure paths produce operational records (ADR-012); the suite
             never writes them to storage/logs. Tests that assert on records
             capture them explicitly. -->
        <env name="LOG_CHANNEL" value="null"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
    </php>
</phpunit>
